# Channel data is ephemeral — don't track message history
channels/*.jsonl

# Keep the directory itself
!channels/.gitkeep

# Boot scripts and PID files are auto-generated per hire, ephemeral
.boot-*.sh
.pid-*

# Python
__pycache__/
*.pyc
.pytest_cache/

# HIVE database
hive.db
